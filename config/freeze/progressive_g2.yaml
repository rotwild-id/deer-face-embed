# @package job.embedder_config.backbone_freeze
# Progressive unfreezing: frozen → group 2 (mid-level layers).
# Tests whether mid-level feature adaptation matters more than top-level.
initial:
  trainable_groups: []
transition_epoch: 30
final:
  trainable_groups: [2]
